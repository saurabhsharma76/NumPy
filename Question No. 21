''' Write a Python function using NumPy to create an array of integers within a
specified range (inclusive) with a given step size '''

import numpy as np

def create_range_array(start,stop,step):
    #create an array of integers within a specified range
    return np.arange(start,stop+step, step)

#example
arr=create_range_array(1,10,2)
print(arr)
